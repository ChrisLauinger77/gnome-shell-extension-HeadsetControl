name: ESLint

on:
    push:
        branches: ["main"]
    pull_request:
        branches: ["main"]

jobs:
    eslint:
        name: Run eslint scanning
        runs-on: ubuntu-latest
        permissions:
            contents: read
            security-events: write
            # Required for private repositories by github/codeql-action/upload-sarif
            actions: read
        steps:
            - name: Checkout
              uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

            - name: Install
              run: npm ci

            - name: Lint
              env:
                  SARIF_ESLINT_IGNORE_SUPPRESSED: "true"
              run: npx eslint .
                  --format @microsoft/eslint-formatter-sarif
                  --output-file eslint-results.sarif
              continue-on-error: true

            - name: Report
              uses: github/codeql-action/upload-sarif@7188fc363630916deb702c7fdcf4e481b751f97a # v4
              with:
                  sarif_file: eslint-results.sarif
                  wait-for-processing: true

    shexli:
        name: Run Shexli scanning
        runs-on: ubuntu-latest
        permissions:
            contents: read
        steps:
            - name: Checkout
              uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

            - name: Set up Python
              uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
              with:
                  python-version: "3.14"

            - name: Install Shexli
              run: pip install --no-cache-dir "tree-sitter<0.26" shexli

            - name: Lint
              shell: bash
              run: |
                  set +e
                  shexli "$GITHUB_WORKSPACE/HeadsetControl@lauinger-clan.de" 2>&1 | tee shexli-output.txt
                  status=${PIPESTATUS[0]}

                  {
                      echo "## Shexli results"
                      echo '```text'
                      cat shexli-output.txt
                      echo '```'
                  } >> "$GITHUB_STEP_SUMMARY"

                  exit "$status"
